/**
 * Placeholder landing page. The real UI (Light Mode Skeumorphism, URL input, live progress,
 * Brand Kit viewer) lands in Phase 5b — this phase (5a) is the API only.
 */
export default function HomePage() {
  return (
    <main>
      <h1>BrandKit AI</h1>
      <p>The API is live. The web UI lands in Phase 5b.</p>
    </main>
  );
}
