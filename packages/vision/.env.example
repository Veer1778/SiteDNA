# Required for AnthropicVisionProvider (only read when it actually makes a request — importing
# this package, or using FakeVisionProvider, never needs a key).
ANTHROPIC_API_KEY=

# Optional. Defaults to a current Claude model if unset.
VISION_MODEL=claude-sonnet-5
