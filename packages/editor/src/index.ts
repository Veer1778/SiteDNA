/**
 * Package identity, used by tooling (CI, dependency-cruiser) to confirm the package builds and
 * is importable. The Fabric.js editor lands in Phase 7 — see Claude.md.
 */
export const PACKAGE_NAME = "@brandkit/editor" as const;
